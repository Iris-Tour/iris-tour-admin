type StoreAdminMutation = {
    firstname: string;
    lastname: string;
    email: string;
    roles: Array<number>;
};

type StoreAdminPromise = {
    message: string;
};

type SuspendAdminPromise = {
    message: string;
};
